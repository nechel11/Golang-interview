http - протокол для определения структуры запросов и ответов браузера, 
		основной строительный блок REST-сервисов

Методы http :
	GET - получить информацию о ресурсе
	POST - создать новый ресурс
	PUT - обновить существующий ресурс 
	DELETE - удалить ресурс


HTTP (HyperText Transfer Protocol) - это протокол передачи данных, который используется для обмена данными в Интернете. Существует несколько версий HTTP, каждая из которых имеет свои особенности и различия.

1. HTTP/1.0
Первая версия HTTP, выпущенная в 1996 году. Она была простой и не имела многих функций, 
которые мы сегодня считаем стандартом. Она использовала одно соединение на каждый запрос/ответ и не поддерживала кэширование и сжатие данных.

2. HTTP/1.1
Выпущена в 1999 году, HTTP/1.1 была значительно улучшена по сравнению с первой версией. 
Она включала поддержку кэширования, сжатия данных, постоянных соединений, 
пайплайнинга (pipelining) и других функций, которые позволяли ускорить передачу данных.

3. HTTP/2
Выпущена в 2015 году, HTTP/2 была разработана для улучшения производительности и безопасности передачи данных. 
Она использует протокол бинарного формата, который позволяет уменьшить размер передаваемых данных и ускорить их передачу. 
HTTP/2 также поддерживает мультиплексирование (multiplexing), 
что позволяет одновременно передавать несколько запросов и ответов через одно соединение.

4. HTTP/3
Выпущена в 2020 году, HTTP/3 - это новая версия протокола, которая основана на протоколе QUIC (Quick UDP Internet Connection). 
HTTP/3 использует UDP вместо TCP, что позволяет уменьшить задержки и улучшить производительность при передаче данных. 
HTTP/3 также поддерживает мультиплексирование и шифрование по умолчанию.

Каждая новая версия HTTP была разработана для улучшения производительности и безопасности передачи данных. 
Однако, в связи с тем, что HTTP/1.0 и HTTP/1.1 все еще широко используются, 
веб-серверы и клиенты должны поддерживать все версии HTTP для обеспечения совместимости с различными приложениями и сервисами.